import Navbar from '../components/Navbar'; // Navbar 경로가 맞는지 확인
import styles from './style.css'; // 스타일을 적용할 경우 사용할 수 있습니다.

export default function Projects() {
  return (
    <div>
      <Navbar /> {/* Navbar 추가 */}
    </div>
  );
}
